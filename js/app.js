var app = new AppView({
  collection: new Weather()
});

// how views receive data
// the only thing that will need the data it the APP VIEW

// this app BOOTSTRAPS the app, associating the collection
